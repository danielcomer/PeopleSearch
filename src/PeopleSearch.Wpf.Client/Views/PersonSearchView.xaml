<UserControl x:Class="PeopleSearch.Wpf.Client.Views.PersonSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=PersonSearchViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Label>Search</Label>
            <TextBox Text="{Binding SearchString}" MinWidth="100"></TextBox>
        </StackPanel>
        <ListBox Grid.Row="1" ItemsSource="{Binding Path=People.Result}"></ListBox>
        <DockPanel Grid.Row="2">
            <Button DockPanel.Dock="Right" Command="{Binding SearchCommand}">Search</Button>
        </DockPanel>
    </Grid>
</UserControl>
